numpy
opencv-python
matplotlib
Pillow
# Optional (recommended for the best water-boundary quality; falls back to
# Canny edge detection automatically if not installed). Note SAM3 itself
# requires torch -- only needed if you use this optional path:
# sam3  -- https://github.com/facebookresearch/sam3
